apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - secret.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - hpa.yaml
  - pdb.yaml
  - monitoring.yaml

commonLabels:
  app: devops-ai-platform
  version: v1.0.0

images:
  - name: devops-ai-platform
    newTag: latest
